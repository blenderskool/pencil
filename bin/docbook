#!/usr/bin/env node
const path = require('path');

let opt = process.argv[2];

const defOpt = 'dev'
const options = ['build', 'init', defOpt];

if (options.includes(opt)) {
  process.argv.splice(2, 1);
}
else {
  opt = defOpt;
}

require(path.join(__dirname, 'docbook-'+opt));